\begin{algorithm}[H]
  \KwIn{En node $v$ (som ikke er \Null)}
  \KwOut{Utfør en operasjon på $v$ først og barna til $v$ etterpå}
  \proc{$\Preorder(v)$}{
    Operate on $v$ \;
    \For{$c \in v.\Children$}{
      $\Preorder(c)$
    }
  }
  \caption{Preorder traversering}
\end{algorithm}
