\begin{algorithm}[H]
  \KwIn{En node $v$ (som ikke er \Null)}
  \KwOut{Utfør en operasjon på barna til $v$ først og $v$ etterpå}
  \proc{$\Postorder(v)$}{
    \For{$c \in v.\Children$}{
      $\Postorder(c)$
    }
    Operate on $v$ \;
  }
  \caption{Postorder traversering}
\end{algorithm}
