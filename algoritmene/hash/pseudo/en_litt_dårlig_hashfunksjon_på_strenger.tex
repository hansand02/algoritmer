\begin{algorithm}[H]
  \KwIn{En streng $s$ og et positivt heltall $N$}
  \KwOut{Et heltall $h$ slik at $0 \leq h < N$}
  \proc{$\HashStringBad(s, N)$}{
    $h \gets 0$\;
    \For{every letter $c$ in $s$}{
      $h \gets h + \charToInt(c)$\;
    }
    \Return $h \bmod N$
  }
  \caption{En litt dårlig hashfunksjon på strenger}
\end{algorithm}
